server {
    listen 80;
    server_name localhost vjgu.online;
    server_tokens off;

    # Certbot challenge path
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all HTTP to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name vjgu.online;
    server_tokens off;

    # SSL configuration (Certbot will populate these paths)
    ssl_certificate /etc/letsencrypt/live/vjgu.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vjgu.online/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Increase client max body size for video uploads
    client_max_body_size 1G;

    # Main application proxy
    location / {
        proxy_pass http://app:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Serve generated media files directly
    location /videos/ {
        alias /app/generated_media/;
        autoindex off;
        expires 7d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin *;
        
        types {
            video/mp4 mp4;
            video/quicktime mov;
            image/png png;
            image/jpeg jpg jpeg;
        }
    }

    location /assets/ {
        alias /app/assets/;
        autoindex off;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
